# Testing — moved

This file previously duplicated `docs/TESTING.md` and referenced the removed
workflow `test-combinations.yml` ("6 templates, 38 extensions"). It is kept as
a redirect to avoid breaking old links.

**See [`docs/TESTING.md`](./docs/TESTING.md) for the current testing guide** —
local `file://` usage, CI layers (L0–L3), and validation commands.

> Historical note: `TESTING.md` at the repository root was deleted in favor of
> `docs/TESTING.md` during the W4 docs drift fix. Do not recreate it; update
> `docs/TESTING.md` instead.
