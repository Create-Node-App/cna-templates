/**
 * Empty module for Vite resolve.alias (stub Node built-ins in Storybook browser bundle).
 * Postgres and other deps use "import x from 'net'", so we need a default export.
 */
export default {};
