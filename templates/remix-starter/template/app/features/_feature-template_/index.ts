/**
 * Feature template — copy this folder when adding a new business capability.
 *
 * Routes import from the public API only:
 *   import { exampleLabel } from '~/features/my-feature';
 *
 * See docs/PROJECT_STRUCTURE.md for conventions.
 */

export const exampleLabel = 'Replace with feature exports';
