import path from 'path';
import react from '@vitejs/plugin-react';
import { defineConfig } from 'vite';

export default defineConfig({
  plugins: [react()],
  resolve: {
    alias: {
      '<%= scope%>ui': path.resolve(__dirname, '../../packages/ui'),
    },
  },
  server: {
    host: '0.0.0.0',
    port: 3000,
  },
});
